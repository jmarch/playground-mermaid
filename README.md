# playground-mermaid

A basic repo for converting a single Mermaid document to a diagram or for running a Docker container to use the Mermaid Live Editor.

**Note:** for run_one.sh use, you'll need to ensure the data sub-directory is a shared Docker Resource, so that it can be mounted as a volume
